const navs=[...document.querySelectorAll('.nav')],views=[...document.querySelectorAll('.view')];function show(id){views.forEach(v=>v.classList.toggle('active-view',v.id===id));navs.forEach(n=>n.classList.toggle('active',n.dataset.target===id));window.scrollTo({top:0,behavior:'smooth'})}document.querySelectorAll('[data-target]').forEach(el=>el.addEventListener('click',()=>show(el.dataset.target)));document.querySelector('.menu').addEventListener('click',()=>document.querySelector('.sidebar').classList.toggle('open'));const toast=document.querySelector('#toast');function pop(){toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3200)}document.querySelectorAll('#newRequest,#newRequest2').forEach(x=>x.addEventListener('click',pop));document.querySelector('.logout').addEventListener('click',()=>{toast.textContent='Sign out is ready to connect to your tenant account.';pop()});
